import PlayArrowRounded from '@mui/icons-material/PlayArrowRounded'; // STYLED COMPONENTS

import { StyledButton, StyledRoot } from './styles';
export default function Section3() {
  return <StyledRoot>
      <StyledButton>
        <PlayArrowRounded fontSize="large" />
      </StyledButton>
    </StyledRoot>;
}